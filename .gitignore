node_modules/
.next/
data/
.env.local
public/icons/*.png
*.tsbuildinfo
next-env.d.ts
certs/

# caché del server de prueba y basura de macOS
.next-test/
.DS_Store

# carpetas de build alternativas (auditoría / instancias de prueba)
.next-audit/
.next-up/

# Las dos que usan los scripts del 03/08. `build:check` borra la suya al
# terminar, pero si el build falla queda; `dev:2` deja la suya siempre.
.next-verify/
.next-2/

# El settings del buscador lleva un secret generado en cada máquina.
# Ver docker/searxng/settings.example.yml
docker/searxng/settings.yml
